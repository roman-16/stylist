/* eslint-disable max-len */
import styled from 'styled-components';

export default styled.section`
  /* linear-gradient is needed to show an opacity */
  background: linear-gradient(rgb(255 255 250 / 50%), rgb(255 255 255 / 50%)), linear-gradient(283deg, #8fd3f4, #84fab0);
`;
