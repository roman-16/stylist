import styled from 'styled-components';
import { Label } from '@shared/components';

export default styled(Label).attrs({ font: 's' })`
  display: block;
  font-weight: bold;
  margin-bottom: 4px;
`;
