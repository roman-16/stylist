#import "./product.graphql"

fragment productWithVariants on Product {
  ...product

  variants {
    nodes {
      id
      stock
      sizes {
        name
        size
      }
    }
    sizes
  }
}
